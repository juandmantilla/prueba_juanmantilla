server:
  port: 8080
spring:
  application:
    name: "PruebaJuanMantilla"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"

adapters:
  r2dbc:
    host: ep-small-art-adtoogeh-pooler.c-2.us-east-1.aws.neon.tech
    port: 5432
    database: sistema_franquicias
    schema: sch_franquicias
    username: neondb_owner
    password: npg_TKLhg3HeEsr5
    sslmode: require

